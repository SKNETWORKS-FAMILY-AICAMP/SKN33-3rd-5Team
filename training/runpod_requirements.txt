# RunPod에서 Qwen3-4B 조건 추출 QLoRA를 학습·추론하는 의존성입니다.
# A40 Pod의 CUDA 12.8 드라이버와 호환되는 Linux x86_64 wheel을 명시한다.
# 이 파일은 RunPod GPU 학습 전용이므로 macOS/Windows의 루트 requirements.txt에는 넣지 않는다.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128 ; platform_system == "Linux" and platform_machine == "x86_64"
transformers>=4.51,<5
huggingface_hub>=0.34,<2
# RunPod에서 HF_HUB_ENABLE_HF_TRANSFER=1을 사용할 때 필요한 고속 다운로드 선택 의존성.
hf_transfer>=0.1,<1
datasets>=3.6,<5
accelerate>=1.6,<2
peft>=0.17,<1
trl>=0.26,<0.29
bitsandbytes>=0.46,<1
pydantic>=2.8,<3
PyYAML>=6.0,<7
