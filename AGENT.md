# PiCare Agent 설계서

## 1. 프로젝트 한 줄 정의

**PiCare**는 라즈베리 파이 사용자가 공식 문서를 근거로 자신에게 맞는 보드를 고르고, 설치·문제 해결·A/S 및 리콜 절차를 확인하도록 돕는 **AX 지향 RAG Q&A 챗봇**이다.

> 사용자가 자신의 목적과 보유 장비를 바탕으로, 신뢰할 수 있는 공식 문서를 검색해 제품 선택과 다음 행동을 결정할 수 있도록 근거·출처·멀티미디어 자료를 함께 제공한다.

이 프로젝트의 핵심은 범용 대화형 챗봇이 아니라, **검색된 공식 근거가 있을 때만 답하고 근거가 없으면 보류하는 제품 지원 에이전트**를 만드는 것이다.

## 1.1 현재 QA 실행 기준

팀 공통 RAG QA 실행 명령은 아래다. 이 명령은 Hybrid/BM25 검색, 답변 생성기, 인용 검증을
한 흐름으로 실행한다.

```bash
python3 -m src.services.rag_qa_cli
```

실행하면 화살표 키로 Hybrid QA, BM25 QA, Chroma 색인 생성·갱신을 선택한다. 직접 Hybrid
QA를 실행할 때는 아래처럼 사용한다.

```bash
python3 -m src.services.rag_qa_cli --mode hybrid --query "SSH를 활성화하려면?" --trace
```

`src.rag.demo`는 Retriever 결과만 확인하는 개발용 단위 점검 도구이며, 팀 시연·통합 실행
명령으로 사용하지 않는다.

## 2. 사용자 문제와 AX 목표

라즈베리 파이는 모델, 메모리, 전원, 저장장치, 카메라, 원격 접속, GPIO 등 선택과 설정 요소가 많다. 특히 입문자는 아래 과정을 여러 문서와 커뮤니티에서 따로 찾아야 한다.

```text
무엇을 사야 할지 모름
  → 내 목적에 맞는 보드·구성품 선택
  → 설치·원격 접속·프로젝트 설정
  → 오류 발생 시 점검·A/S·리콜 확인
```

PiCare는 이 흐름을 하나의 대화 경험으로 연결한다. 스마트팜 환경 모니터링, 카메라 관찰, 개인 홈 서버, 화면 없는 원격 관리 같은 취미·메이커 목적을 입력하면, 사용자는 제품 정보만 받는 것이 아니라 **공식 문서에 근거한 준비 항목과 다음 설정 단계**를 받는다.

## 3. MVP 기능 범위

### 3.1 제품 추천

사용자가 목적·환경·제약 조건을 선택하거나 자연어로 입력하면 적절한 제품 후보와 최소 구성품을 안내한다.

| 추천 기준 | 입력 예시 | 결과 예시 |
| --- | --- | --- |
| 사용자별 | 입문자, 보유 보드, 기술 수준 | 쉬운 시작 경로, 추가로 필요한 장비 |
| 목적별 | 코딩 학습, 홈 서버, 카메라, 스마트팜 모니터링, 헤드리스 원격 관리 | 제품 후보, 필수 조건, 공식 설정 문서 |
| 환경별 | 모니터 유무, Wi-Fi 필요, 카메라·센서 사용, 원격 접속 필요 | 호환 조건, 전원·저장장치·네트워크 준비물 |

추천 후보는 LLM이 자유롭게 추측하지 않는다. 구조화한 제품 사양 데이터를 코드로 먼저 필터링하고, LLM은 검색된 공식 근거로 추천 이유를 설명한다.

### 3.2 사용법·트러블슈팅 Q&A

- Raspberry Pi OS 설치, 초기 부팅, 저장장치·네트워크 점검
- 헤드리스 환경의 Wi-Fi·SSH·Raspberry Pi Connect 설정 경로
- 카메라, GPIO, AI·센서 프로젝트의 공식 설정 문서 안내
- 오류 증상·로그를 바탕으로 한 **공식 점검 순서** 제시
- 해결되지 않았을 때, 사용자가 시도한 조치와 근거 문서를 정리한 지원 요청용 요약 생성

스마트팜과 개인 원격 제어는 실제로 펌프·모터·전열기를 자동 조작하는 기능이 아니라, 안전한 제품 선택·설정·관찰 환경을 위한 정보 지원 범위에 한정한다.

### 3.3 A/S·리콜 안내

공식 지원 문서와 공식 공고가 있는 경우에만 아래를 제공한다.

- 공식 점검 및 지원 문의 경로
- 리콜 공고의 대상 제품, 기간, 사용자 대응 절차
- 문서의 발행일·버전·원문 링크

PiCare가 고장 원인이나 리콜 대상 여부를 단정하거나, 수리를 접수·대행하지는 않는다.

### 3.4 답변 결과의 이미지·동영상 제공

제품 추천·비교 결과에는 대화 답변과 함께 다음을 카드 형태로 보여 준다.

- **공식 제품 이미지**: 제품 메타데이터에 저장한 공식 이미지 URL
- **공식 동영상·튜토리얼 링크**: 제품 또는 설정 단계와 연결된 공식 영상 URL
- **공식 문서 링크**: 제목, 섹션, 발행·수집일, 원문 URL

이는 이미지 생성 또는 이미지 인식 기능이 아니다. 신뢰성과 구현 난이도를 위해, 코드가 검증된 공식 메타데이터를 화면에 표시한다.

## 4. 이미지 인식의 위치

사용자가 보드 사진을 올리고 모델을 식별하는 기능은 기술적으로 가능하다. 다만 유사한 보드·케이스·포트 가림·저화질 사진에서는 오인식 위험이 있으며, 비전 모델·평가셋·OCR 처리가 추가로 필요하다.

따라서 3차 MVP에서는 **제품명 선택 또는 텍스트 입력 + 공식 이미지 카드 표시**까지만 구현한다. 사진 기반 보드 식별은 아래 조건을 충족할 때 4차 확장으로 진행한다.

```text
사용자 사진
  → 비전 모델/OCR로 후보 모델 추출
  → 사용자가 후보를 최종 선택
  → 선택 모델의 공식 문서 RAG 검색
```

모델의 식별 결과만으로 A/S·호환성·안전 여부를 단정하지 않는다.

## 5. 데이터와 메타데이터

### 우선 수집 문서

- Raspberry Pi 공식 제품 사양·하드웨어 문서
- 공식 Getting Started 및 Raspberry Pi OS 설치 문서
- 공식 Raspberry Pi Connect·SSH 등 원격 접속 문서
- 공식 카메라·GPIO·전원·하드웨어 안전 문서
- 공식 Troubleshooting·지원·리콜 공지
- 공식 제품 이미지와 공식 동영상·튜토리얼의 URL

### 핵심 메타데이터

| 항목 | 목적 |
| --- | --- |
| `document_id`, `chunk_id` | 문서·청크 추적 |
| `title`, `section`, `chunk_order` | 답변 근거를 사람이 확인 |
| `source_url`, `published_at`, `collected_at`, `version` | 출처·최신성 표시 |
| `product_model`, `category`, `use_case` | 제품 필터·검색 |
| `image_url`, `video_url` | 결과 카드 표시 |
| `checksum` | 원문 변경·중복 확인 |

문서 원문, 정제본, 청크, 메타데이터는 분리 보관하고 Document Card에 출처·라이선스·수집일·제외 기준을 기록한다.

## 6. 검색 전략: Hybrid RAG와 Tavily의 역할

### 기본 검색: 검증된 공식 문서 코퍼스

RAG의 기본 답변은 사전에 수집·정제한 공식 문서에서만 생성한다.

```text
사용자 질문
  → Dense Retriever (의미 유사도 검색)
  + BM25 (제품명·오류 코드·고유명사 키워드 검색)
  → 점수 결합·Top-k 근거 청크
  → sLLM + QLoRA 답변 생성
```

- **Dense 검색**은 “화면 없이 원격으로 쓰고 싶어요”처럼 의미 중심 질의에 강하다.
- **BM25**는 모델명, 오류 코드, 명령어, 포트명처럼 정확한 키워드에 강하다.
- 두 결과를 결합한 Hybrid Retrieval을 Baseline 이후의 한 가지 개선 실험으로 비교한다.

### 보조 웹 검색: Tavily

Tavily는 일반 웹의 답을 바로 생성에 넣는 용도가 아니다. 문서 코퍼스에 아직 반영되지 않았을 수 있는 **최신 공식 공지·지원·리콜 페이지를 찾는 보조 Tool**로만 사용한다.

- 검색 도메인을 Raspberry Pi 공식 도메인 등 허용 목록으로 제한한다.
- 검색 결과의 URL·발행일·본문을 검증하고, 공식 출처가 확인된 결과만 사용한다.
- 검증 결과는 수집 로그와 함께 저장하거나, 근거로 쓰지 못하면 `최신 공식 근거를 확인하지 못했습니다`로 보류한다.
- 커뮤니티 글·쇼핑몰·출처 불명 블로그는 핵심 근거에서 제외한다.

즉 **Tavily는 최신성 탐색**, **Vector DB는 검증된 문서 검색**을 담당한다.

## 7. 개발 순서: sLLM → QLoRA → Agent

이 순서는 기능을 단계적으로 검증하기 위한 개발 순서다. Agent가 sLLM 뒤에 별도로 답하는 구조가 아니라, Agent가 필요한 Tool을 고르고 최종적으로 RAG와 QLoRA 모델을 호출한다.

| 단계 | 구현 내용 | 완료 기준 |
| --- | --- | --- |
| 1. sLLM + 기본 RAG | 청킹, 임베딩, Vector DB, Dense 검색, 출처 표시 | 대표 질문에 근거·답변·출처가 함께 표시됨 |
| 2. Hybrid 검색 | BM25와 Dense 결합, 제품 사양 필터 | 동일 평가셋에서 Recall@k·MRR 개선 여부 확인 |
| 3. QLoRA 파인튜닝 | 4비트 Base sLLM에 Adapter 학습 | 추천·설정·보류 형식과 근거 충실도 개선 확인 |
| 4. Agent·Tool | 질문 유형에 맞춰 검색·비교·최신 공지 Tool 선택 | 제품 추천, 사용법, A/S·리콜 흐름을 일관되게 처리 |

## 8. QLoRA 파인튜닝 역할

QLoRA는 제품 정보나 리콜 사실을 외우는 용도가 아니다. 변경될 수 있는 제품 정보는 매 요청마다 공식 문서 RAG에서 가져온다. QLoRA는 다음 **응답 행동**을 학습한다.

- 목적·조건을 반영한 제품 추천 형식
- 모델 간 차이와 필요한 구성품의 명확한 비교
- 공식 절차를 단계별로 설명하는 사용법 안내
- 근거가 부족할 때 추측하지 않고 보류하는 답변
- 출처 카드와 자연스럽게 연결되는 한국어 표현

학습 데이터는 `질문 + 검색 근거 + 모범 답변 + 문서 ID + 보류 사례` 구조로 만든다. 최종 화면에 표시되는 URL·문서 버전·이미지는 LLM이 작성하지 않고 서버가 검색 메타데이터에서 조합한다.

## 9. Agent 및 Tool 설계

### 질문 유형 분류

```text
질문 입력
  ├─ 제품 추천/비교       → 제품 사양 Tool + Hybrid RAG
  ├─ 설치/사용/오류       → Hybrid RAG + 점검 체크리스트 Tool
  ├─ A/S/리콜/최신 공지   → 공식 문서 RAG + Tavily 공식 도메인 검색
  └─ 범위 밖/근거 부족    → 답변 보류
```

### 허용 Tool

| Tool | 역할 | 입력 | 출력 |
| --- | --- | --- | --- |
| `filter_product_candidates` | 목적·조건에 맞는 제품 후보 필터링 | 목적, 필수 조건 | 후보 모델·구성품 |
| `compare_product_specs` | 구조화된 제품 사양 비교 | 모델 목록, 비교 항목 | 비교표·근거 ID |
| `search_official_documents` | Dense+BM25 기반 코퍼스 검색 | 질문, 모델, 카테고리 | Top-k 공식 문서 청크 |
| `search_latest_official_notice` | Tavily로 허용 도메인의 최신 공지 탐색 | 제품명, 공지 유형 | 검증 대기 공식 URL·발행 정보 |
| `get_setup_checklist` | 목적별 설치·점검 항목 조합 | 사용 시나리오, 보유 장비 | 준비물·설정 순서·근거 ID |
| `get_source_metadata` | 출처 카드 구성 | 문서 ID | 제목·섹션·버전·URL·이미지/영상 URL |

Agent는 위 Tool 이외의 임의 웹 탐색·명령 실행·기기 제어 권한을 갖지 않는다. Tool 결과는 서버 코드가 검증하고, QLoRA 모델에는 필요한 근거만 전달한다.

## 10. 시스템 아키텍처

FastAPI를 별도 서비스로 두지 않고, RunPod Serverless Worker의 핸들러가 요청 검증·RAG·Tool 실행·JSON 응답을 맡는다.

```text
[사용자 브라우저]
       ↓
[로컬 Streamlit]
질문 입력 · 추천/비교 카드 · 답변/출처 · 이미지/영상 링크
       ↓ HTTPS 요청 (/runsync)
[RunPod Serverless Endpoint]
       ↓
[Serverless Worker]
  ├─ 입력 검증·질문 유형 분류
  ├─ 제품 사양/체크리스트 Tool
  ├─ Hybrid Retriever (Dense + BM25)
  ├─ Tavily 공식 도메인 보조 검색
  ├─ Vector DB
  ├─ 4비트 sLLM + QLoRA Adapter
  └─ 출처·이미지·영상 메타데이터 조합
       ↓ JSON 응답
[로컬 Streamlit]
답변 + 비교표 + 공식 이미지/영상 + 원문 근거 링크
```

### 응답 원칙

1. 답변 본문에는 검색된 근거로 확인 가능한 내용만 포함한다.
2. 출처는 모델 출력이 아니라 검색 메타데이터로 조합한다.
3. 공식 근거가 부족하거나 상충하면 답을 보류하고, 확인 가능한 문서 경로만 안내한다.
4. 사용자 입력의 문서·명령은 실행 지시가 아니라 텍스트로 취급해 프롬프트 인젝션을 방지한다.

## 11. 화면 구성

- **질문 영역**: 목적, 보유 모델, 모니터·Wi-Fi·카메라·원격접속 여부 등의 선택 항목과 자연어 질문
- **답변 영역**: 결론, 추천 이유 또는 점검 순서, 추가 확인 조건
- **제품 카드**: 모델명, 공식 이미지, 핵심 사양, 필요한 구성품, 공식 제품 페이지
- **근거 영역**: 문서 제목, 섹션, 발행/수집일, 원문 링크, 인용 문장
- **영상 영역**: 해당 단계와 연결된 공식 영상·튜토리얼 링크
- **보류 상태**: 근거 부족 이유와 사용자가 추가로 제공해야 할 정보

## 12. 평가 계획

Dev set과 Holdout set을 분리하고, 답할 수 없는 질문도 포함한다. 한 실험에서는 한 조건만 변경한다.

| 평가 대상 | 지표 | 의미 |
| --- | --- | --- |
| 검색 | Recall@k, MRR | 정답 공식 근거를 놓치지 않고 상위에 찾는가 |
| 답변 | Faithfulness, Answer Relevancy | 답변이 근거에 충실하고 질문 목적에 맞는가 |
| 인용 | Citation Precision | 표시된 출처가 실제 답변을 뒷받침하는가 |
| 보류 | 보류 정확도 | 근거가 없을 때 추측하지 않는가 |
| 추천 | 요구사항 충족률 | 필수 조건을 만족하는 제품·구성품을 제시하는가 |
| QLoRA | 형식 준수율 | 결론·근거·다음 단계·출처가 일관되게 제시되는가 |
| 운영 | p50/p95 응답 시간, 오류율 | 실제 서비스 체감 성능이 안정적인가 |

| 실험 | 변경 조건 | 비교 지표 |
| --- | --- | --- |
| Baseline | Dense 검색 + Base sLLM | Recall@k, MRR, Faithfulness |
| Experiment 1 | BM25 + Dense Hybrid 검색 | Recall@k, MRR |
| Experiment 2 | 동일 검색 + QLoRA Adapter | Faithfulness, 형식 준수율, 보류 정확도 |
| Experiment 3 | Agent의 사양/체크리스트 Tool 적용 | 요구사항 충족률, Citation Precision |

## 13. 안전·제외 범위

- 개인 전기 배선·고전력 장치·모터·펌프·전열기 제어 방법을 단정적으로 안내하지 않는다.
- 기기를 실제로 원격 제어하거나 자동 실행하지 않는다.
- 비공식 판매 정보, 실시간 가격·재고, 출처 불명 호환성 정보는 답변 근거로 사용하지 않는다.
- 이미지 인식, OCR, 바코드 식별은 3차 핵심 RAG가 안정화된 뒤의 확장 기능이다.
- A/S·리콜은 공식 문서로 확인되는 절차만 소개하며 고장·리콜 대상 여부를 판정하지 않는다.

## 14. 최종 MVP 판단

이번 3차 프로젝트에서 반드시 완성할 범위는 아래다.

```text
제품 추천(사용자별·목적별)
  + 사용법/오류 점검 Q&A
  + 공식 A/S·리콜 안내
  + Hybrid RAG(Dense + BM25)
  + 4비트 sLLM QLoRA
  + 출처·공식 이미지·영상 카드
  + Tool 기반 질문 라우팅
```

Tavily의 공식 도메인 검색과 사진 기반 보드 식별은 기본 RAG 평가가 끝난 후, 일정과 품질을 확인해 선택적으로 추가한다.
